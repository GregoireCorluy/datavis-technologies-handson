<script>
    const myRadius = 1;
    function circumCircle(radius){
        let answ = 2*Math.PI*radius;
        return answ.toFixed(2);
    }
</script>

The cirucmference of a circle with radius {myRadius}m is {circumCircle(myRadius)}m.